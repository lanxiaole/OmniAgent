# uv包管理器部署问题

**setuptools 的 flat-layout 自动发现限制**

当你没有显式声明 `packages` 时，setuptools 会扫描项目根目录，自动把含有 `.py` 文件或子目录的文件夹识别为候选 Python 包。你的根目录下同时存在 `agent_core`、`backend`、`frontend`、`assets`、`docker`，setuptools 发现了多个"看起来像包"的顶级目录。

但 **flat-layout（扁平布局）不允许自动发现多个顶级包** —— 这是 setuptools 的安全机制，防止误把 `tests/`、`docs/`、`assets/` 这类非包目录打包进发布版本。所以它会直接抛错退出。

**解决方案**：显式告诉 setuptools 哪些是你要打包的包，哪些不是。即通过 `[tool.setuptools]` 段声明 `packages` 和 `py-modules`。





| #    | 问题                                 | 根因                                                  | 修复方案                                                     |
| ---- | ------------------------------------ | ----------------------------------------------------- | ------------------------------------------------------------ |
| 1    | **400 错误（DataInspectionFailed）** | 搜索结果中含敏感内容，阿里云百炼内容安全检测拦截      | `_sanitize_text()` 清洗控制字符 + `_truncate_content()` 截断 500 字符 + `errors.py` 友好错误提示 |
| 2    | **搜索结果时间不准（返回 2025 年）** | `search()` 没传 `time_range` 和 `topic` 参数          | 新增 `time_range`/`topic` 参数 + `_infer_search_params()` 智能推断（新闻类自动用 `topic="news"` + `time_range="day"`） |
| 3    | **文件路径错误（写入到项目根目录）** | `_safe_path()` 用 `os.path.abspath()` 不展开 `~`      | 加 `os.path.expanduser(path)` 先展开 `~`                     |
| 4    | **代码无限重试烧钱**（之前的问题）   | 原代码靠文本提示让 Agent 自重试，Agent 不遵守         | 物理锁定：连续失败 3 次后相似代码**直接拒绝调用 subprocess**，相似度阈值 0.4 |
| 5    | **Agent 自写爬虫**（之前的问题）     | 危险代码黑名单不完整，`urllib`/`httpx`/`aiohttp` 漏网 | 扩充黑名单封堵所有常见网络库 + system.txt 强约束 + 标注"自写爬虫会触发锁定" |



### 💡 经验 1：信任 LLM 不如信任代码

> **错误假设**："我在 prompt 里告诉 Agent 最多重试 3 次，Agent 会遵守。" **现实**：Agent 会"贴心"地继续尝试，因为它认为"再试一次说不定就成功了"。 **解决**：把约束写在代码里。`_is_locked()` 检查是**强制性的**，不是建议。

### 💡 经验 2：堵死比教育更有效

> **错误做法**：在 prompt 里说"不要用 requests 写爬虫"。 **更好做法**：在 `python_executor.py` 黑名单里直接拦截 `requests.get(`、`import requests`。 **核心**：Agent 看不到你写在哪一层，它只知道"我尝试了但被拒了"，于是下次会走工具。

### 💡 经验 3：让约束有可见的后果

> 重试锁定时，**不调用 subprocess**，所以**不会消耗任何执行资源**。这是关键 —— 不是"警告 Agent 不要做"，而是"做了也没用"。

### 💡 经验 4：黑盒外部服务的兜底

> 阿里云百炼的内容安全检测是黑盒，**我们无法控制它的策略**。 应对：清洗 + 截断 + 友好错误提示。三层防护都不到位时，至少让用户知道发生了什么。

### 💡 经验 5：缓存是最低成本的优化

> 7 天搜索缓存 + 30 天网页缓存，让重复查询**完全免费**。Tavily 每月 1000 积分在有缓存的情况下，实际可支撑的查询量是 5-10 倍。

---

# Electron + Python 打包模式日志冲突问题

**问题**：应用打包后，再次启动时后端崩溃，前端显示 "Internal Server Error"

| #    | 问题                                 | 根因                                                  | 修复方案                                                     |
| ---- | ------------------------------------ | ----------------------------------------------------- | ------------------------------------------------------------ |
| 1    | **前端显示 Internal Server Error**   | 后端日志初始化 PermissionError 导致进程崩溃          | 启动前清理残留进程 + 日志写入失败回退机制                    |
| 2    | **日志文件偶发 PermissionError**     | RotatingFileHandler 文件锁 + 旧进程未正确退出         | 启动前 taskkill 清理 backend.exe + 等待 500ms 释放锁         |
| 3    | **关闭后残留进程**                   | Windows 子进程清理不彻底，SIGTERM 对子进程树无效       | 3 秒超时后 taskkill /F /PID /T 强制清理子进程树              |
| 4    | **多进程日志文件名冲突风险**         | 秒级时间戳可能重复（极端场景同秒重启）                | 日志文件名添加 PID 后缀，操作系统保证唯一                    |

### 💡 经验 6：文件锁 ≠ 文件名冲突，警惕错误归因

> **错误归因**："旧进程占用了日志文件名，导致新进程无法创建同名文件。" **现实**：`PermissionError` 不是因为文件名冲突，而是 `RotatingFileHandler` 在初始化时会打开文件获取锁、扫描目录检查备份，这个过程中旧进程残留的**文件句柄**导致 Windows 拒绝操作。即使文件名完全不同，只要旧进程持有该目录下任何文件的句柄，新进程就可能无法在同一目录创建文件。
> 
> **教训**：看到 `PermissionError: [Errno 13] Permission denied` 时，不要立刻假设是文件名冲突。先检查：① 是否有残留进程？② 是否有文件句柄未释放？③ 操作系统的文件锁机制是否干扰？

### 💡 经验 7：PID 后缀不是银弹，清理才是根本

> **PID 后缀的真实价值**：解决极端场景（同秒快速重启）和提供可追溯性（知道哪个进程创建的日志）。但它**不能**解决端口占用、文件句柄残留等问题。
> 
> **真正起作用的是**：启动前 `taskkill /F /IM backend.exe /T` 清理残留进程 + 等待 500ms 让文件锁释放。这才是从根源解决问题。

### 💡 经验 8：开发模式 vs 生产模式的隐性差异

| 对比项 | 开发模式 (`npm run dev`) | 打包模式 (`npm run dist`) |
|--------|------------------------|--------------------------|
| 进程管理 | 手动启动/关闭，可见性强 | Electron 自动 spawn，后台静默 |
| 端口检测 | 有 `checkPortInUse()` 保护 | 没有检测，直接启动 |
| 日志可见性 | 终端直接显示 | 需打开开发者工具 F12 |
| 错误反馈 | 控制台直接报错 | 窗口只显示 "Internal Server Error" |
> 
> **教训**：开发模式正常 ≠ 生产模式正常。打包应用需要额外关注：① 子进程生命周期管理 ② 文件系统权限 ③ 进程间通信 ④ 错误日志可见性。

### 💡 经验 9：防御性编程的三层结构

> 对于"进程启动可能失败"这类问题，应该有三层防御：
> 1. **事前预防**：启动前清理残留资源（`cleanupStaleBackends()`）
> 2. **事中兜底**：核心功能失败时降级运行（日志文件写入失败 → 回退到控制台模式）
> 3. **事后恢复**：提供手动恢复手段（删除旧日志、重启应用）
> 
> 缺少任何一层，应用在生产环境中都可能出现无法恢复的故障。